def count_in_list(lst, value) -> int:
    """Conta quantas vezes um valor aparece em uma lista.

    Args:
        lst (list): A lista na qual procurar o valor.
        value: O valor a ser contado na lista.

    Returns:
        int: O número de vezes que o valor aparece na lista.
    """
    return lst.count(value)
